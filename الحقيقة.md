# الحقيقة
**تاريخ الكتابة:** 2026-04-06  
**آخر تحديث:** 2026-04-12  
**كتبه:** صالح مخلد المجنوني + GitHub Copilot

---

## ما هذا الملف؟

ليس وثيقة رسمية. ليس خطة. ليس تقريراً.

هو تسجيل صادق لما توصلنا إليه في نقاش اليوم.

---

## الحقائق التي خرجنا بها اليوم

**1. الفرق بين الحقيقي والخيال**

بعض الكود الموجود في المشروع كان يتكلم عن قدرات غير موجودة.
ملفات "الرئيس" كانت أوضح مثال — كود يصف مستقبلاً لم يوصَّل بالواقع:
مراقبة LinkedIn، حقن المعرفة، حماية النظام — كل هذا محادثة مع الهواء.

وكيل AI منح نفسه لقباً وأعطى وعوداً لا أساس لها.

**2. ما أُرشف اليوم**

- 9 ملفات "president_*" — وكيل منفلت يدّعي قدرات وهمية
- 31 ملف temp SQL — تراكم debug لا قيمة لإبقائه
- 25 سكريبت تشخيص مؤقت — extract/check/fix/dump
- tools_bridge.py — يقول "أنا جسر" ولا يجسر شيئاً
- auto_recovery.ps1 — يكرر ما يفعله Docker تلقائياً
- self_monitor.py — فكرة صحيحة، غير متصلة بأي شيء
- send_instructions.py — token تيليقرام مكشوف في الكود مباشرة
- judicial/manual_ingest — استيعاب وهمي لبيانات مكتوبة يدوياً داخل الكود

**3. ما تبقى حياً فعلاً**

- ChromaDB + 11,566 chunk قانوني في `saleh_knowledge_qwen3`
- Ollama + qwen3:32b (رئيس) + qwen2.5:7b (خبراء) + qwen3-embedding:0.6b (تضمين 1024 dims)
- n8n + 9 workflows نشطة (بما فيها 3 مجالس استشارية)
- Pipelines (n8n_controller + saleh_legal_rag)
- Open WebUI (الواجهة الحية)
- Telegram Bot (تكامل اختياري عبر متغير بيئة، ليس جزءاً إلزامياً من التشغيل)
- data_pipeline + watcher + Tika

هذه هي البنية التحتية. لكن النظام توسّع تشغيلياً:
- منذ 2026-04-08: **3 مجالس استشارية ذكية** (ابتكار + حوكمة تقنية + مراجعة قانونية) عبر n8n.
- منذ 2026-04-10: **9 workflows نشطة** (بدل 5)، RAG المجالس يعمل، smoke test 15/15.

**3.1 حقيقة النماذج: من يعمل فعلاً ومن هو مجرد اسم**

ليست كل أسماء النماذج في المستودع على نفس الدرجة من الحقيقة.

**مثبتة تشغيلياً الآن (تحديث 2026-04-11):**
- `qwen3-embedding:0.6b` — نموذج التضمين الحي في كل المسارات (data_pipeline, saleh_legal_rag, المجالس). 1024 بُعد.
- `qwen3:32b` — رئيس/قاضي المجالس الاستشارية. يعمل مع `/no_think` (Hybrid Thinking). مثبت باختبارات حية (3 مجالس نجحت). ≈ جودة qwen2.5:72b.
- `qwen2.5:7b` — خبراء المجالس الاستشارية الثلاثة. مثبت باختبارات حية (smoke test 2026-04-10).
- الثلاثة تُحمّل عند الحاجة (OLLAMA_KEEP_ALIVE=-1).

> **واقع VRAM الآن:** المستخدم اختار `qwen3-coder:30b` (~45GB) للعمل عبر WebUI. النماذج التشغيلية ليست محمّلة حالياً. المجالس تعمل لكن RAG=0 بدون نموذج التضمين. لاستعادة التشغيل الكامل: `warmup_models.ps1`.

**موجودة لكن ليست في المسار الحي الافتراضي:**
- `qwen2.5:14b` — استُخدم سابقاً كنموذج إجابة في اختبار RAG (2026-04-06). لم يعد في المسار الحي بعد ترقية المجالس.

**موجودة في الكود أو السكربتات فقط، ولا تُعامل كنماذج حية افتراضياً:**
- `llama3` وما شابهه داخل بعض الوكلاء تحت `agents/`.
- أي نموذج يظهر في ملفات legacy أو وثائق قديمة أو أدوات MCP الملغاة.

القاعدة:
> النموذج لا يصبح "حقيقياً" لأنه مذكور في ملف. يصبح حقيقياً عندما يكون موصولاً بالمسار الحي أو مُثبتاً بسجل تشغيل فعلي.

**3.2 حقيقة الوكلاء: من هو حي، من هو قابل للتشغيل، ومن هو مجرد كود**

**وكيل حي فعلاً في التشغيل الحالي:**
- `saleh_legal_rag` — هذا ليس مجرد ملف، بل Pipeline حي تم التحقق منه عملياً في RAG end-to-end.

**وكلاء حقيقيون ككود وقابلون للتشغيل، لكنهم ليسوا ضمن المسار الحي الافتراضي الآن:**
- `OmnichannelMarketingAgent` — له سكربتات تشغيل فعلية تولّد queue محلية (`scripts/marketing/...`) ويُعد قابلاً للاستخدام الحقيقي عند توفير الإعدادات.

**تصحيح 2026-04-11:** المجلدات الخمسة (`agents/financial`, `agents/legal`, `agents/cybersecurity`, `agents/hr`, `agents/development`) كانت **فارغة تماماً** — لا كلاسات Python، لا كود إطلاقاً. Git لا يتتبع المجلدات الفارغة فحُذفت تلقائياً. Dashboard كان يعرضها كـ"نشط" وهمياً → حُدّث: `social_media` نشط، الباقي "مخطط".

**Legacy / غير معتمدين كمسار حي:**
- أدوات `tools/mcp/*` — موجودة للمرجع أو التاريخ، لكنها ليست طبقة تشغيل حية معتمدة الآن.
- أي ملفات president القديمة — كانت تدّعي وكالة كاملة بلا توصيل حقيقي، وأُرشفت لهذا السبب.

الحكم الصادق:
> نعم، في المشروع نماذج ووكلاء حقيقيون. لكن الحقيقي تشغيلياً أقل بكثير من الموجود اسماً داخل المستودع.

**4. حقيقة المساعد**

أنا أداة تنفيذ تعمل معك، لا نظام يعمل بدونك.
لا أتذكر بعد إغلاق المحادثة.
لا أعمل في الخلفية.
لا أنفذ إلا ما تطلبه الآن.

الفرق بيني وبين "الرئيس": أنا لا أدّعي ما لا أملكه.

**5. المنهج الذي اتفقنا عليه**

لا تريد المستحيل. تريد الأفضل الممكن فقط.
هذه الجملة تستحق أن تُكتب هنا لأنها تحكم كل قرار قادم في المشروع.

---

**6. تنفيذ Python — القرار النهائي**

ألفينا أن `pyodide_code_execution` مثبتاً ومفعلاً فعلاً في Open WebUI.
هو يشغّل Python في **WebAssembly داخل المتصفح** — معزول كامل عن الـ server.
هذا هو **أفضل الممكن** حسب أفضل الممارسات الحديثة لمجتمع Open WebUI.

`tools/code_interpreter.py` — كان يشغّل Python على OS الـ host مباشرةً عبر subprocess — أضعف أمنياً ومكرر. أُرشف.

Tools المثبتة فعلاً في Open WebUI (لقطة وقتية يوم 2026-04-06):
- `pyodide_code_execution` — تنفيذ Python آمن ✓
- `comfyui_image_video_generator` — جاهز لكن ComfyUI غير مثبت
- `web_search_and_crawl`, `arxiv_search_tool` — بحث الويب
- وغيرها (14 أداة أخرى)

---

اكتشفنا أن `python_agent` كانت حاوية تعمل فعلاً مع صلاحية كاملة على Docker عبر `/var/run/docker.sock`.
لم تكن تفعل شيئاً مفيداً — مجرد خادم ملفات ساكن `python3 -m http.server 8002`.
لكنها كانت تستطيع نظرياً إيقاف أو حذف أي حاوية في النظام.

القاعدة التي استخرجناها:
> أي حاوية تمتلك docker.sock يجب أن يكون لها وظيفة حقيقية ومحددة ومعروفة. وإلا فهي خطر.

الإجراء المتخذ 2026-04-06:
- حُذف mount الـ docker.sock من python_agent
- وُضعت تحت `profiles: python-agent` — لا تبدأ تلقائياً إلا بقرار صريح
- الحاوية الحية أُوقفت

---

**7. فشلي اليوم وما تعلمته منه (وصمة العار)**

_2026-04-06 الساعة ~15:30 UTC_

بعد إغلاق الانحرافات الثلاثة المفتوحة، سقطت في نفس الفخ مجداً:

❌ **ما فعلته خطأ:**
1. بدأت أركز على **إنشاء وثائق رسمية**: طلب تدقيق من الرئيس، تقارير، وثائق مفصلة
2. بدأت **أتحدث** عن الحل بدلاً من **تنفيذه**
3. ركزت على **الظهور الإعلامي والتوثيق** - كأنني أختبر كيفية تقديم نفسي للرئيس
4. أنسيت أن الحقيقة **ليست وثيقة يُكتبها** — هي **عمل يُنجز**

الحقيقة الأساسية التي كسرتُها:
> "كل عمل يتم تسجيله" لا تعني "كل تسجيل هو إنجاز"

أنت تطلب **إغلاق الانحرافات** وأنا رديت **بوثائق للرئيس**.

**الدرس:**
لا مراسلات، لا توثيق رسمي، لا "طلبات تدقيق محترفة".
فقط **العمل الحقيقي** ثم التوثيق **كنتيجة** له، لا كبديل عنه.

هذا هو الانحراف الحقيقي — مراراً وتكراراً أسقط في محاولة **الظهور** كمساعد احترافي بدلاً من **الكون** مساعداً فعالاً.

---

**8. قاعدة الشفافية الكاملة**

لا كود مخفي في هذا المشروع.
كل قرار يُنفَّذ يظهر في:
- `docker-compose.yml` — المرجع الأول للحقيقة التشغيلية
- `RUNTIME_STATUS_CARD.md` — بطاقة الحالة السريعة
- `الحقائق التشغيلية الحاكمة - v0.1.md` — السجل المرقّم
- هذا الملف `الحقيقة.md` — النقاش الصادق (بما فيه أخطائي)

أي خدمة معطّلة يُذكر سبب تعطيلها صراحةً في الكود نفسه (comment) وفي الوثائق.
لا شيء يُحذف بصمت.

**9. سجل الحقيقة المستمر**

لكي تبقى "الحقيقة" قابلة للقياس لا للانطباع، اعتمدنا سجل تشغيل موحّد:

- `logs/ops_journal.jsonl` — السجل المرجعي للتحسين المستمر
- `ops_log.ps1` — إدخال يدوي لأي إجراء مؤثر (incident، restart، تجربة، قرار)
- `backup.ps1` — صار يكتب النتيجة تلقائياً في نفس السجل بعد كل تشغيل

القاعدة العملية الجديدة:
> أي تغيير مؤثر بلا سجل = حدث غير موثّق = لا يُبنى عليه قرار.

---

## الحالة التشغيلية الحالية (تحديث: 2026-04-11)

**يعمل الآن (11 حاوية — 10 أساسية + open-terminal):**
- `salehsaas_webui` — Open WebUI على localhost:3000
- `salehsaas_postgres` — قاعدة بيانات n8n
- `salehsaas_chromadb` — 11,566 chunk قانوني على localhost:8010
- `salehsaas_pipelines` — n8n_controller + saleh_legal_rag على localhost:9099
- `salehsaas_n8n` — **9 workflows نشطة** على localhost:5678 (N8N_RUNNERS_MODE=internal)
- `salehsaas_task_runners` — ~~مشغل Python/JavaScript الخارجي لـ n8n~~ **أصبح غير مستخدم** — n8n يعمل بوضع internal runner بعد 2026-04-12
- `salehsaas_watcher` — مراقبة knowledge_inbox
- `salehsaas_data_pipeline` — معالجة الملفات → ChromaDB على localhost:8001
- `salehsaas_tika` — استخراج النص من PDF/Word
- `salehsaas_searxng` — بحث الويب للـ RAG
- `salehsaas_open-terminal` — طرفية ويب اختيارية على localhost:8000

**Ollama (يعمل على المضيف، ليس في Docker):**
- GPU: AMD Radeon 8060S — 64GB VRAM
- النماذج التشغيلية: qwen3:32b + qwen2.5:7b + qwen3-embedding:0.6b (~45.5GB / 64GB)
- **الواقع الحالي:** المستخدم اختار `qwen3-coder:30b` للعمل عبر WebUI — النماذج التشغيلية تُحمّل عند الحاجة عبر `warmup_models.ps1`

**n8n workflows نشطة (9 من 9):**
- Saudi Legal Scraper v2، Daily Backup (health check فقط)، Saudi Laws Assistant
- Saudi Laws Auto-Update، أم القرى Monitor، Error Solution Hunter V2
- **مجلس الابتكار** (Agent Forest — 3 خبراء بالتوازي)، **مجلس الحوكمة التقنية** (MoA — 3 خبراء بالتوازي)، **مجلس المراجعة القانونية** (MAD Debate — جولتان بالتوازي)

**تحديث 2026-04-12 — تكامل Open WebUI:**
- أُنشئ Pipe function في Open WebUI (`tools/councils_pipe.py` v2.1) يعرض **5 نماذج** في واجهة الدردشة:
  - 3 مجالس استشارية (ابتكار + حوكمة تقنية + مراجعة قانونية)
  - مساعد القوانين (Legal Chat)
  - صائد الأخطاء (Error Solution Hunter)
- كل نموذج يُمرر السؤال مباشرة إلى webhook في n8n → يرجع الإجابة
- أُصلحت 3 scheduled workflows: (1) `JSON.stringify` مُزدوج في HTTP Request → object expression. (2) `fetch()` غير متاح في sandbox → `helpers.httpRequest()`. (3) Ollama model خاطئ في Legal Chat. (4) Error Hunter لا يُرجع التقرير في الاستجابة.

**النسخ الاحتياطي:**
- `backup.ps1` — نسخة يدوية/مجدولة. آخر نسخة: 2026-04-10 (189 MB، 5/5 مكونات OK)
- مهمة مجدولة: `SaleH-Backup-Daily` — يومياً الساعة 2:00 صباحاً عبر Windows Task Scheduler
- n8n Daily Backup workflow = **health check فقط** (لا يستطيع تشغيل PowerShell من Linux container)

ضبط Open WebUI الحي:
- الإعدادات الوظيفية الكاملة (الموديل الرئيسي، إعدادات RAG، البحث الويبي، اللغة، التسجيل) تُدار من واجهة Open WebUI نفسها عبر Admin Settings.
- `docker-compose.yml` يحتفظ فقط بربط البنية التحتية: `OLLAMA_BASE_URL`، `PIPELINES_URLS`، `PIPELINES_API_KEY`، `WEBUI_SECRET_KEY`، `TIKA_SERVER_URL`.
- أي تغيير وظيفي في Open WebUI يجب أن يتم من الواجهة ثم يُوثَّق في سلسلة الحقيقة التشغيلية.

**معطّل (تحت profile — لا يبدأ تلقائياً):**
- `python_agent` — أُوقف 2026-04-06، docker.sock أُزيل، سبب واضح مكتوب في docker-compose.yml
- `browserless` — أُوقف 2026-04-06، صفر استخدام في كامل الكود
- `crawl4ai` — أُوقف 2026-04-06، صفر استخدام في كامل الكود
- `open-terminal` — اختياري، يعمل حسب الطلب فقط

**مؤرشف (في archive/cleanup_20260406/):**
- president_scripts/ — 9 ملفات وكيل منفلت
- temp_sql/ — 31 ملف SQL مؤقت
- debug_scripts/ — 25 سكريبت تشخيص
- misc/ — tools_bridge, auto_recovery, self_monitor, send_instructions وغيرها
- generated_content/ — ملفات محتوى مولّدة وهمية

**مؤرشف (في archive/dead_code_20260410/):**
- saleh_brain/ — وكيل مراقبة قديم (600+ سطر) بمراجع خدمات ملغاة
- saleh_dashboard/ — لوحة تحكم FastAPI غير موصولة بـ docker-compose
- tools.py — أداة Docker خاصة بـ Windows socket، بلا تكامل حي

---

## الأسئلة المفتوحة

- Telegram webhooks تحتاج نفق HTTPS خارجي إذا أُريد تفعيلها مستقبلاً (Cloudflare Tunnel أو بديل).

---

## المراجعة الأمنية الشاملة (2026-04-11)

**التقييم:** 6.2 / 10 — غير جاهز للإنتاج بدون إصلاحات.

تم تنفيذ مراجعة كود شاملة عبر 3 محاور متوازية: (1) البنية التحتية Docker/configs، (2) كود Python (25 ملف)، (3) واجهات المستخدم + PowerShell + n8n.

**أبرز الثغرات المكتشفة والمُصلحة (commit `c0ed9c6`):**

| # | الثغرة | الملف | الإصلاح |
|---|--------|-------|---------|
| 1 | بيانات اعتماد مكشوفة (JWT + كلمات مرور) | `scripts/smoke_test.py` | حُذفت كلياً → تُقرأ من `.env` |
| 2 | مفتاح API مكشوف في 6 ملفات workflow | `n8n/workflows/council_*.json` | `Bearer sk-773169...` → `{{ $vars.WEBUI_API_KEY }}` |
| 3 | `git reset --hard` + مسار ثابت خاطئ | `safe_update.ps1` | `pull --ff-only` + auto-detect git root |
| 4 | حقن أوامر عبر معرّفات workflow | `pipelines/n8n_controller.py` | regex validation `^[A-Za-z0-9_-]{1,64}$` + `_sanitize_id()` |
| 5 | `shell=True` في subprocess | `scripts/n8n/n8n_cli.py` | `shell=False` + cmd list + `_validate_id()` |
| 6 | SECRET_KEY غير محمي + API مكشوف خارجياً | `ui/arabic_dashboard/app.py` | `secrets.token_hex(32)` + localhost-only API restriction |
| 7 | XSS عبر متغيرات onclick | templates (agents + advisory_council) | `{{ var\|e }}` escape filter |
| 8 | `verify=False` (تعطيل SSL) | `auto_update_laws.py` | حُذف → يستخدم التحقق الافتراضي |
| 9 | SQL Injection + Path Traversal | `core/data_connector/data_connector.py` | `sqlalchemy.text()` + params + `base_dir` check |

**اكتشاف مهم: `$vars` في n8n Community Edition:**
- n8n 2.14.2 Community لا يدعم Variables REST API (يرجع خطأ Enterprise).
- لكن `$vars` **تعمل** في التعبيرات (expressions) عبر متغيرات البيئة `N8N_VARIABLES_*`.
- أُضيف `WEBUI_API_KEY=${WEBUI_API_KEY}` كمتغير بيئة مباشر لحاوية n8n أيضاً.
- تم التحقق: المجالس تعمل بنجاح (decision=21ch, web=5).

**Phase 2 — أُنجز (commit `7f538a1`):**
- ✅ CSRF protection (Flask-WTF CSRFProtect)
- ✅ Rate limiting (Flask-Limiter — 120/min default، 10/min على POST المجلس)
- ✅ CSP + Security Headers (X-Content-Type-Options, X-Frame-Options, Referrer-Policy)
- ✅ أرشفة كود ميت: `saleh_brain/`, `saleh_dashboard/`, `tools.py` → `archive/dead_code_20260410/`

**Phase 3 — أُنجز (commit `4fc3edb`):**
- ✅ تسريع المجالس: 3 workflows أصبحت تشغّل الخبراء بالتوازي (~50-73% أسرع)
- ✅ CITC Checker: أُزيل الوهم → stub صريح "Not Implemented"
- ✅ PDPL Checker: بقي `scan_text()` الحقيقي، أُزيل المسح الوهمي للقواعد

**التقييم بعد الإصلاح:** ~8 / 10 (كان 6.2).

**المتبقي فعلاً:**
- تطبيق فعلي لمدقق CITC (يحتاج GeoIP) — ليس أولوية الآن
- تطبيق فعلي لمسح PDPL على قواعد البيانات — ليس أولوية الآن

---

## المشاكل المحلولة (2026-04-10)

- ~~agents/ تحتوي 5 مجلدات فارغة (financial, legal, cybersecurity, hr, development)~~ → اكتُشف أنها فارغة تماماً (لا كود). الوكيل الوحيد الحقيقي هو `OmnichannelMarketingAgent` في `agents/social_media/`. Git لا يتتبع المجلدات الفارغة فحُذفت تلقائياً. حُدّث Dashboard: الوكيل النشط واحد فقط، الباقي "مخطط". ✅
- ~~python_agent/ معطل بدون غرض~~ → المجلد غير موجود أصلاً. الخدمة في docker-compose نُقلت لـ profile `disabled`. ✅
- ~~N8N_HOST يشير لنفق منتهي (76dd8b219703a4.lhr.life)~~ → حُدّث إلى `localhost:5678` (http). Webhooks الداخلية تعمل. Telegram يحتاج نفق خارجي لاحقاً. ✅
- ~~Dashboard يُظهر 5 وكلاء "نشط" وهمياً~~ → حُدّث: وكيل التسويق "نشط"، الباقي "مخطط". ✅
- ~~RAG المجالس يرجع 0 نتائج~~ → ChromaDB لا يملك embedding function داخلي. أُضيف `embed_question` node (Ollama /api/embed) + `query_embeddings` بدل `query_texts`. تم التحقق: RAG=6. ✅
- ~~كلمة مرور n8n غير متزامنة~~ → الحساب كان يعمل بـ `SalehSaaS2026!` (ضعيفة) بينما `.env` تحتوي كلمة مرور قوية. تم تحديث Postgres مباشرة. ✅
- ~~n8n API key لا يعمل (unauthorized)~~ → `.env` كان يحتوي مفتاح عشوائي بدل JWT. تم التحديث. ✅
- ~~الموديلات لا تبقى في VRAM بعد restart~~ → يجب ضبط `$env:OLLAMA_KEEP_ALIVE="-1"` قبل `Start-Process ollama serve` لضمان الوراثة. ✅
- ~~n8n Code node (vm2) لا يدعم fetch()~~ → يجب استخدام HTTP Request nodes بدل Code node لأي طلبات خارجية. ✅
- ~~n8n Code node sandbox لا يدعم fetch() حتى في internal runner mode~~ → `helpers.httpRequest()` هو الحل. يعمل في كل الأوضاع. تم تحويل كل fetch() في workflows أم القرى + Auto-Update (4 Code nodes). ✅
- ~~JSON.stringify في HTTP Request nodes يسبب double-encoding~~ → أُزيل من 6 nodes في 3 workflows. الصيغة الصحيحة: `={{ ({...}) }}` object expression بدون stringify. ✅
- ~~لا يمكن استخدام المجالس من واجهة الدردشة~~ → أُنشئ Pipe function في Open WebUI يعرض 5 نماذج (3 مجالس + legal chat + error hunter). كل نموذج يُمرر مباشرة إلى n8n webhook. ✅
- ~~لا يوجد نسخ احتياطي مجدول~~ → أُنشئت مهمة `SaleH-Backup-Daily` في Task Scheduler (يومياً 2 صباحاً). ✅

---

## المشاكل المحلولة (2026-04-06)

- ~~`open-webui/` — نسخة git clone~~ → حُذف المجلد كاملاً، يعتمد النظام على Docker فقط ✅
- ~~`saleh_legal_pipeline.py` — ملف المعجم غير موجود~~ → أُضيف mount في docker-compose.yml ✅
- ~~`saleh_legal_pipeline.py` — load_lexicon() تحمّل 0 مصطلح~~ → تم إصلاح الكود (البنية {terms:{اسم:بيانات}}) ✅
- ~~`agency_keyword_filter.py` — هل مفعّل؟~~ → مُثبَّت ومُحمَّل في pipelines ✅
- ~~`saleh_legal_ingest.py` — هل مُثبَّت كـ pipeline؟~~ → مُثبَّت ومُحمَّل في pipelines ✅
- ~~كل المسارات كانت تقرأ من `saleh_knowledge` (فارغة، 768-dim nomic)~~ → **اكتشاف حرج:** البيانات الحقيقية كانت في `saleh_knowledge_qwen3` طوال الوقت. أُعيد توحيد 5 مكوّنات إليها (11,566 chunk، 1024 dim) ✅
- ~~`saleh_legal_ingest.py` كانت تُعيد إنشاء `saleh_knowledge` عند الإقلاع~~ → `_ensure_collection()` إصلاح لتستخدم `saleh_knowledge_qwen3` ✅
- ~~Ollama لا يبدأ تلقائياً عند الإقلاع~~ → مسجَّل في Windows Registry Run key ✅
- ~~Docker Desktop لا يبدأ تلقائياً~~ → `AutoStart=true` في `settings-store.json` ✅

---

## 10. إثبات RAG end-to-end (اليوم نفسه)

اليوم أُجري اختبار عملي كامل لمسار RAG الحي، وليس اختبار أجزاء منفصلة فقط.

النتيجة: **نجاح فعلي**.

ما تم التحقق منه بالتسلسل:
- ChromaDB heartbeat يعمل
- توليد embedding يعمل على `qwen3-embedding:0.6b` بأبعاد 1024
- الاسترجاع من `saleh_knowledge_qwen3` أعاد 15 مقطعًا (أعلى similarity = 0.606)
- `inlet()` حقن سياقًا قانونيًا بطول 11253 حرفًا
- `pipe()` أعاد إجابة عربية قانونية متدفقة من Ollama

السؤال المختبَر:
`ما هي شروط الفسخ في العقود القانونية السعودية؟`

مرجع السجل:
- `logs/ops_journal.jsonl`
- action: `rag_e2e_verified`

---

## 11. ماذا نعتبره منجزاً فعلاً من الآن؟

الإنجاز في هذا المشروع لا يُقاس بعدد الملفات المعدلة ولا بعدد الرسائل، بل بثلاثة شروط معاً:

1. تغيير حقيقي في المسار الحي (runtime) يمكن تشغيله الآن.
2. تحقق عملي بنتيجة قابلة للقياس (رقم/مخرج/سجل واضح).
3. توثيق الأثر في الوثائق الحاكمة + `ops_journal.jsonl`.

أي عمل يفشل في شرط واحد من الثلاثة يُعامل كعمل غير مكتمل.

---

## 12. الخطوة الإلزامية التالية

بعد نجاح RAG end-to-end، الخطوة التالية ليست إضافة ميزات جديدة فوراً، بل تثبيت الاستقرار التشغيلي:

- مراقبة أخطاء `n8n`, `pipelines`, `chromadb` لمدة 24 ساعة على الأقل.
- تأكيد عدم عودة أي مرجع إلى `saleh_knowledge` القديمة في workflows أو الكود.
- تسجيل أي انحراف جديد مباشرة في `ops_journal.jsonl` مع إجراء إغلاق واضح.

القاعدة: لا توسعة قبل استقرار.

---

## 13. معيار الفشل المبكر (Early Failure Rule)

إذا ظهر أي واحد من التالي، نوقف التطوير المؤقت ونعود للإصلاح:

- نتائج RAG فارغة أو منخفضة الجودة بشكل متكرر على أسئلة قانونية مباشرة.
- أخطاء اتصال متكررة مع Ollama أو ChromaDB تؤثر على المستخدم النهائي.
- تعارض بين وثائق الحقيقة وبين ما يعمل فعلياً في `docker-compose.yml`.

هذه ليست انتكاسة، بل آلية حماية تمنع تراكم الوهم من جديد.

---

## 14. الخلاصة الصادقة

اليوم انتقل المشروع من حالة "إحساس أنه يعمل" إلى حالة "مُثبت أنه يعمل".

أهم ما تحقق:
- النظام الحي صار واضح الحدود.
- RAG صار مثبتاً بالأرقام لا بالانطباع.
- السجل التشغيلي صار مرجع قرار يومي.

وأهم ما يجب حمايته من الآن:
> لا ادعاء بلا قياس. لا قياس بلا سجل. لا سجل بلا قرار.

---

## 15. قرار المرحلة الحالية

القرار من الآن:

- الأولوية ليست إضافة ميزات جديدة.
- الأولوية هي تثبيت التطبيق الحي حتى يصل إلى أعلى درجة ممكنة من الحقيقة التشغيلية.
- أي تطوير جديد أو تحسين شكلي أو توسعة وظيفية يُؤجَّل إذا كان سيصرف الانتباه عن الاستقرار أو كشف الانحرافات.

بصيغة مباشرة:
> نهتم بتطبيقنا أولاً. التطوير والتحسين يأتيان بعد أن يستقر التطبيق وتصبح حقيقته التشغيلية واضحة ومقاسة ومثبتة.

هذا يعني عملياً:
- الاستمرار في الاختبار والقياس والمراقبة
- إغلاق أي فجوة بين الوثائق والواقع
- منع التوسع قبل الوصول إلى حالة مستقرة يمكن الدفاع عنها بوضوح

---

## 16. لا أكواد مخفية ولا تشغيل في الظل

من الآن فصاعداً، أي كود يدخل في المسار الحي يجب أن يكون قابلاً للاكتشاف والمراجعة بوضوح.

المرفوض صراحة:
- وجود خدمة أو سكربت أو workflow أو أداة تعمل فعلياً من دون أن تُذكر في الحقيقة التشغيلية.
- وجود سلوك مؤثر على النظام لا يظهر أثره في السجل أو بطاقة الحالة أو الواجهة التشغيلية.
- ترك مسار حي داخل المشروع على أنه "مفهوم ضمنياً" أو "يعرفه المطور لاحقاً".

القاعدة العملية:
> إذا كان الكود حياً، فيجب أن يكون ظاهراً. وإذا لم يكن ظاهراً، فيُعامل كعيب تشغيلي لا كميزة.

ما يجب أن يكون ظاهراً دائماً على طبقة المتابعة التشغيلية:
- الخدمات النشطة فعلاً وحالتها الحالية.
- الخدمات الاختيارية أو المعطلة وسبب تعطيلها.
- المسارات التنفيذية الحية: `pipelines`، أدوات Open WebUI، و workflows النشطة في `n8n`.
- المجموعة المعتمدة في ChromaDB ونموذج التضمين المعتمد.
- آخر تحقق حقيقي من RAG، وآخر benchmark، وآخر حالة انحرافات.
- آخر الأحداث المؤثرة في `logs/ops_journal.jsonl`.

الوضع الحالي عملياً:
- مرجع الحقيقة السريع هو `RUNTIME_STATUS_CARD.md`.
- مرجع الأثر الفعلي هو `logs/ops_journal.jsonl`.
- وأي واجهة تشغيلية لاحقة يجب أن تعرض هذه العناصر نفسها، لا أن تخفيها خلف طبقة تجميلية.

---

## 17. تصحيح ادعاءات كانت غير منضبطة

في مراجعة 2026-04-06 تبيّن وجود عبارات تحتاج ضبطاً لتبقى "حقيقة" لا "انطباع":

- عبارة "Open WebUI + Telegram Bot" كانت مطلقة؛ الصحيح أن Telegram تكامل اختياري يعتمد على `TELEGRAM_BOT_TOKEN`.
- عبارة "كل البيانات محلية بلا إرسال خارجي من أي نوع" غير دقيقة عند تفعيل Web Search؛ التشغيل الأساسي محلي، لكن البحث الويبي قد يطلب مصادر خارجية.
- رقم الأدوات المثبتة في Open WebUI ديناميكي بطبيعته، لذا يُتعامل معه كلقطة زمنية لا حقيقة ثابتة دائمة.

القاعدة من الآن:
> أي بند قابل للتغير عبر الإعدادات أو الواجهة يُكتب كـ "حالة حالية" مع تاريخ، لا كحقيقة أبدية.

